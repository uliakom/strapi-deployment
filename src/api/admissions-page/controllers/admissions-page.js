'use strict';

/**
 * admissions-page controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::admissions-page.admissions-page');
